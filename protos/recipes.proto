syntax = "proto3";

message ProductId {
  uint32 id = 1;
}

message Recipe {
  uint32 id = 1;
  uint32 productId = 2;
  string title = 3;
  string notes = 4;
}

service Recipes {
  rpc Find(ProductId) returns (Recipe) {}
}
